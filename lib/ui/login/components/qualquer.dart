import 'package:flutter/material.dart';

class Qualquer extends StatelessWidget {
  const Qualquer({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Olá mundo!"));
  }
}